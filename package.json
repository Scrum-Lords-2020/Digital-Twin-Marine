{
	"scripts": {
		"start": "node server/server.js",
		"build": "cd client && npm run build",
		"client": "cd client && npm start",
		"server": "cd server && npm run server",
		"dev": "concurrently --names \"SERVER,CLIENT\" -c \"bgGreen,bgBlue\" \"npm run server\" \"npm run client\""
	},
	"devDependencies": {
		"concurrently": "^5.3.0"
	},
	"engines": {
		"node": "12.x"
	},
	"dependencies": {
		"bcryptjs": "^2.4.3",
		"body-parser": "^1.19.0",
		"concurrently": "^5.3.0",
		"cors": "^2.8.5",
		"express": "^4.17.1",
		"is-empty": "^1.2.0",
		"jsonwebtoken": "^8.5.1",
		"mongoose": "^5.10.7",
		"passport": "^0.4.1",
		"passport-jwt": "^4.0.0",
		"validator": "^13.1.17"
	}
}
